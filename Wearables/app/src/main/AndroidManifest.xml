<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ipn.mx.wearables">

    <!-- Permisos necesarios -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Características del dispositivo -->
    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Biblioteca necesaria para dispositivos Wear OS -->
        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />

        <!-- Indica que la aplicación es independiente en dispositivos Wear OS -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <!-- Actividad principal -->
        <activity
            android:name=".presentation.MainActivity"
            android:exported="true"
            android:taskAffinity=""
            android:theme="@style/MainActivityTheme.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad secundaria -->
        <activity
            android:name=".presentation.WearableActivity"
            android:exported="true"
            android:theme="@style/WearableActivityTheme.Starting">
        </activity>
    </application>

</manifest>